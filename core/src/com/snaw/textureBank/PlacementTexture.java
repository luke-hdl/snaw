package com.snaw.textureBank;

public class PlacementTexture extends TextureBank {
    //Used for placing rooms and overlays (which don't have textures).
    //Things with textures use an internal texture instead.
    //Nothing in particular yet :P
    public PlacementTexture(String renderingLevel) {
        super(renderingLevel);
    }
}
